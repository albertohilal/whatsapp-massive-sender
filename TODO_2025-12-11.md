# TODOs para el 11 de diciembre de 2025

1. **Identificar prospectos con 'Sin rubro' en el frontend**
   - Agregar log temporal en el backend para listar los prospectos que retornan 'Sin rubro' en la respuesta del endpoint `/filtrar-prospectos`.
   - Analizar por qué no hacen JOIN correctamente.

2. **Revisar y depurar consulta SQL de `/filtrar-prospectos`**
   - Asegurar que el JOIN con `ll_societe_extended` y `ll_rubros` siempre retorne rubro si existe.
   - Ajustar la consulta si es necesario.

3. **Revisar y limpiar datos huérfanos en `llxbx_societe`**
   - Eliminar o corregir prospectos en `llxbx_societe` que no tengan extensión en `ll_societe_extended`, si corresponde.

4. **Revisar test de autenticación fallido**
   - Analizar y corregir el test Playwright que falla por error 401 (autenticación).

5. **Revalidar todos los tests Playwright**
   - Ejecutar nuevamente todos los tests tras los ajustes y asegurar que pasen antes de hacer push.
